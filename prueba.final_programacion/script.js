// numeros impares 
function impares(){
    let N = 0;
    let array = [];

    for(let i = 0; i<= N; i++ ){
        array.push(i*1);
        let N = parseInt(prompt("ingrese su lista:"));
        if(N%2 !==0){

        }
    
    }
    alert(`lista completa: ${array}
    lista de impares ${N}`)
}